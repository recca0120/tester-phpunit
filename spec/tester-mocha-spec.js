'use babel';

/* @flow */
describe('when a test is written', () => {
  it('has some expectations that should pass', () => {
    expect('i should write more teste').toNotEqual('this package has a lot of tests :)');
  });
});
